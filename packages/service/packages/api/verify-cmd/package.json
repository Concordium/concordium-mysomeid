{
  "name": "verifier",
  "version": "1.0.0",
  "main": "index.js",
  "license": "Commercial",
  "scripts": {
    "build": "cargo build --manifest-path=Cargo.toml --release",
    "start": "cargo run -- --node http://146.190.94.164:20001 --statement \"$(<test-data/statement.json)\" --address \"$(<test-data/address.txt)\" --proof \"$(<test-data/proof.json)\" --challenge \"$(<test-data/challenge.txt)\" ",
    "dev": "nodemon --watch src -e rs --exec 'yarn start'"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
